import { Link } from 'react-router-dom'
const statusDot = { Ongoing: 'bg-acid', Upcoming: 'bg-clay-400', Completed: 'bg-cream-200' }
export default function ProjectCard({ project, index }) { return <Link to={`/projects/${project.slug}`} data-hover className="group relative block overflow-hidden bg-ink-900 text-cream-50"><div className="aspect-[4/5] overflow-hidden"><img src={project.cover} alt={project.name} loading="lazy" className="h-full w-full object-cover transition duration-700 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:scale-105"/><div className="image-shade absolute inset-0"/></div><div className="absolute inset-x-0 top-0 flex items-center justify-between p-5"><p className="label flex items-center gap-2"><span className={`h-1.5 w-1.5 rounded-full ${statusDot[project.status] || 'bg-cream-50'}`}/>{project.status}</p><span className="label text-cream-50/60">{String((index || 0)+1).padStart(2,'0')}</span></div><div className="absolute inset-x-0 bottom-0 p-5 sm:p-6"><p className="label text-acid">{project.type} / {project.city}</p><div className="mt-3 flex items-end justify-between gap-4"><div><h3 className="font-display text-4xl leading-none tracking-[-.045em]">{project.name}</h3><p className="mt-3 text-sm text-cream-50/65">From <span className="text-cream-50">{project.priceFrom}</span></p></div><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full border border-cream-50/50 transition duration-500 group-hover:rotate-45 group-hover:bg-acid group-hover:text-ink-900">↗</span></div></div></Link> }
